[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    smoke: quick route availability checks
    unit: pure helpers without full HTTP flow
    security: RBAC and tenant isolation
    slow: external mocks or heavy flows
